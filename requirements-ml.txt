# GNM-VLNVerse Baseline — training / inference dependencies
# Install with:  pip install -r requirements-ml.txt
#
# PyTorch must be installed separately before this file because the correct
# wheel depends on your CUDA version and Python version.  See:
#   https://pytorch.org/get-started/locally/
#
# Example (CUDA 12.1, Python 3.10/3.11):
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
#
# After installing PyTorch, install the remaining ML dependencies:
#   pip install -r requirements-ml.txt

# ── Model architectures ───────────────────────────────────────────────────────
timm>=0.9.16               # EfficientNet-B0 and MobileNetV2 pretrained weights

# ── Computer vision ───────────────────────────────────────────────────────────
opencv-python-headless>=4.9
Pillow>=10.0

# ── Numerical ─────────────────────────────────────────────────────────────────
numpy>=1.26
scipy>=1.12

# ── Configuration ─────────────────────────────────────────────────────────────
pyyaml>=6.0
omegaconf>=2.3.0

# ── Experiment tracking ───────────────────────────────────────────────────────
wandb>=0.17.0

# ── Progress bars and logging ─────────────────────────────────────────────────
tqdm>=4.66
rich>=13.7

# ── Data handling ─────────────────────────────────────────────────────────────
pandas>=2.2
h5py>=3.11

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=8.0
pytest-cov>=5.0

# ── Code quality ──────────────────────────────────────────────────────────────
black>=24.3
isort>=5.13
